version: 2

jobs:
  build:

    docker:
      - image: circleci/node:8

    working_directory: /home/website
    steps:
      - checkout
      - run:
          name: Deploying website.
          command: echo Deploying website.

      - persist_to_workspace:
          root: /home/website
          paths:
            - .

  deploy:
  
    docker:
      - image: cibuilds/aws

    steps:
      - attach_workspace:
          at: /home/website

      - run:
          name: deploy to AWS
          command: |
            pwd
            if [ "${CIRCLE_BRANCH}" = "master" ]; then
               aws s3 sync . s3://stepanvalchyshyn.com/ --delete
            else
               echo "Not master branch, dry run only"
            fi

workflows:
  version: 2

  build_deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master